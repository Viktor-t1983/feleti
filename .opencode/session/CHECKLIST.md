# Чек-лист конца сессии

> Выполнять перед завершением работы. Ни один пункт не является опциональным.

## 1. Код

- [ ] Все изменённые файлы сохранены на диск
- [ ] `python -m py_compile` проходит для всех новых .py файлов
- [ ] Нет явных синтаксических ошибок (проверить `docker compose logs backend --tail=20`)

## 2. База данных

- [ ] Если меняли модели — миграция сгенерирована (`alembic revision --autogenerate`)
- [ ] Миграция применена (`alembic upgrade head`)
- [ ] Seed данных актуален (если меняли seed.py — перезапустить)

## 3. Документация

- [ ] `CHANGELOG.md` — добавлена секция текущей сессии в `## [Unreleased]`
- [ ] `TODO.md` — выполненные пункты отмечены ✅, новые добавлены
- [ ] `docs/CONTEXT_HANDOFF.md` — обновлена секция "Сводка последней сессии"
- [ ] `docs/PROJECT_BOOT.md` — обновлены "Текущий статус" и "Следующие шаги"
- [ ] `OPEN_QUESTIONS.md` — новые вопросы записаны
- [ ] `.opencode/session/history/YYYY-MM-DD_HHMM.md` — создан и заполнен

## 4. Git

- [ ] `git add -A`
- [ ] `git status` проверен — нет случайных изменений
- [ ] `git commit -m "type(scope): описание на русском"`
- [ ] `git log --oneline -3` — коммит виден

## 5. Docker (если используется)

- [ ] `docker compose ps` — все нужные сервисы запущены
- [ ] `curl http://localhost:8000/api/v1/health` — backend отвечает
- [ ] Логи backend чистые (`docker compose logs backend --tail=20`)

## 6. Скиллы

- [ ] Если архитектура изменилась — обновлены связанные SKILL.md
- [ ] Если добавили новый домен — создан новый SKILL.md

## 7. Финальная проверка

- [ ] Запущен `python scripts/verify_context.py` — ошибок нет
- [ ] Пользователю сообщён статус (кратко, < 5 строк)
- [ ] Вопросы пользователю заданы явно (если есть)

---

**После выполнения всего чек-листа — сессия считается завершённой.**
Следующая сессия начинается с чтения последнего `history/` файла + CONTEXT_HANDOFF.md.
